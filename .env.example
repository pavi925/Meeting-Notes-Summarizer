# Copy this file to .env and fill in values to enable OpenAI mode.
# Without this, the app runs fully locally with Whisper + BART + FLAN-T5.

OPENAI_API_KEY=sk-your-key-here

# Optional: override the OpenAI model used for summarization/action items
OPENAI_MODEL=gpt-4o-mini
